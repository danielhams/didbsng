--- git-2.23.0/config.mak.uname~	2019-08-16 21:03:03.000000000 +0000
+++ git-2.23.0/config.mak.uname	2019-11-23 13:55:44.683991880 +0000
@@ -324,7 +324,8 @@
 	NO_MMAP = YesPlease
 	NO_REGEX = YesPlease
 	SNPRINTF_RETURNS_BOGUS = YesPlease
-	SHELL_PATH = /usr/gnu/bin/bash
+	SHELL_PATH = $(CONFIG_SHELL)
+	CC = gcc
 	NEEDS_LIBGEN = YesPlease
 endif
 ifeq ($(uname_S),IRIX64)
@@ -342,7 +342,8 @@
 	NO_MMAP = YesPlease
 	NO_REGEX = YesPlease
 	SNPRINTF_RETURNS_BOGUS = YesPlease
-	SHELL_PATH = /usr/gnu/bin/bash
+	SHELL_PATH = $(CONFIG_SHELL)
+	CC = gcc
 	NEEDS_LIBGEN = YesPlease
 endif
 ifeq ($(uname_S),HP-UX)
